 \documentclass[10pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs} 
\usepackage{amsmath}   
\usepackage{siunitx}   
\usepackage[hidelinks]{hyperref}   
\usepackage{caption}   
\usepackage{float}    
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{xcolor}
\microtypesetup{expansion=false}
\definecolor{myblue}{RGB}{0,162,232}
\captionsetup{labelfont={color=myblue}}

\title{\textbf{Structures}}
\author{Group 8}
\date{\today}

\begin{document}

\maketitle

\tableofcontents
\newpage
\newpage
\section{Deliverable 1: Geometry}
The roof geometry was reconstructed from the supplementary drawing using a one-to-one SketchUp model and then transferred into GSA. As shown in Figure~\ref{fig:geometry_overview}(a), points labelled \textit{R} define the roof profile and represent the mid-points of the roof section, while points labelled \textit{S} define the support system and lower restraint points. The reconstructed model was checked against the original drawing on both sides of the roof and the overall geometric deviation was approximately 0.5\%, which is acceptable for global FE modelling \textbf{REFERENCE?}.

The second diagram shows the FE idealisation adopted for analysis. In Figure~\ref{fig:geometry_overview}(b), green denotes beam elements used for the roof profile, red denotes the high tie modelled as a tie element, and blue denotes the support-tree members modelled as bar elements. In GSA, beam elements model axial, bending and torsional effects, bar elements provide axial stiffness only, and tie elements are axial-only elements with zero stiffness in compression. This makes beams suitable for the roof rafters, bars suitable for the tree members, and a tie suitable for the prestressed high tie. Both end supports were modelled as fixed supports, while the pin-connected members were treated consistently on both sides because the connected elements were assumed not to transfer bending moment. This idealisation captures the main load path and preserves the key dimensions required for later loading, validation and mesh sensitivity studies.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth,trim={70 30 60 25},clip]{Weixin Image_20260315183303.png}

\small (a) Reconstructed nodal geometry

\vspace{0.4em}
\includegraphics[width=0.9\textwidth]{element tyep.png}

\small (b) Idealised FE member types
\caption{Geometry adopted for the global model.}
\label{fig:geometry_overview}
\end{figure}


\newpage
\section{Deliverable 2: Material, Section, and Loading Data }
\subsection{Material and Section Properties and Loading Data}

The global FE model was defined using linear-elastic steel. In accordance with the brief, all members were assigned an elastic modulus of \SI{200}{\giga\pascal}, a Poisson's ratio of 0.30 and density of \SI{7850}{\kilogram\per\metre\cubed} were adopted so that self-weight could be included automatically in GSA. S355 steel was used for the primary roof members, which is consistent with the Heathrow T5 project information \textbf{REFERENCE?}. The twin high-tie strands were modelled as a single prestressed tie element with the same total cross-sectional area and an initial prestress of \SI{450}{\mega\pascal}.

Only vertical symmetric loading was considered. The characteristic permanent roof load was taken as the sum of the aluminium standing seam roof (\SI{0.10}{\kilo\pascal}), structural deck (\SI{0.15}{\kilo\pascal}), insulation/acoustic layers (\SI{0.40}{\kilo\pascal}) and the service allowance (\SI{0.15}{\kilo\pascal}), giving $G_k = \SI{0.80}{\kilo\pascal}$. The live load imposed on the roof was taken as $Q_k = \SI{0.60}{\kilo\pascal}$. The governing vertical ultimate limit state (ULS) combination used in the model was
\begin{equation}
1.35G_k + 1.5Q_k
\label{eq:uls_vertical_combination}
\end{equation}
Here, $G_k$ and $Q_k$ were converted to line loads using the projected tributary width carried by one transverse frame. The supplementary roof information states that the main rafters and associated tie-rods are spaced at \SI{18}{\metre} centres, so a tributary width of \SI{18}{\metre} was adopted for the 2D frame idealisation. On this basis, the superimposed permanent roof load is \SI{14.4}{\kilo\newton\per\metre} $(0.80 \times 18)$ and the imposed live load is \SI{10.8}{\kilo\newton\per\metre} $(0.60 \times 18)$, with member self-weight still included separately through the material density. This projected-area treatment is consistent with common roof-loading practice and with the basis commonly used for roof snow loading in Eurocode 1 \cite{bsen_snow}.

The strap members were modelled as bar elements. Therefore, only their axial stiffness was represented in the global model, using an equivalent steel area of $A = 2 \times 500 \times 40 = \SI{40000}{\milli\metre\squared}$. The spacing between the two plates was neglected in this idealisation because the bar elements do not carry bending.

\begin{table}[H]
\raggedright
\caption{Section data adopted for the global model}
\label{tab:section_data}
\renewcommand{\arraystretch}{1.35}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.23\textwidth} >{\raggedright\arraybackslash}p{0.32\textwidth} >{\raggedright\arraybackslash}p{0.39\textwidth}}
\toprule
\textbf{Member} & \textbf{Section} & \textbf{Modelling Note} \\
\midrule
Arch rafter & Welded box, \SI{800}{\milli\metre} wide, \SI{50}{\milli\metre} wall thickness & Depth varies from about \SI{1.8}{\metre} to \SI{3.8}{\metre} along the roof profile \\
Tusk rafter & Welded I-section, \SI{50}{\milli\metre} plate thickness & Overall depth varies from about \SI{0.9}{\metre} to \SI{3.8}{\metre} \\
Legs and arms & Circular Hollow Section (CHS) \SI{914}{\milli\metre} $\times$ \SI{40}{\milli\metre} & Taken directly from the supplementary sketch \\
Wings & CHS \SI{711}{\milli\metre} $\times$ \SI{50}{\milli\metre} & Taken directly from the supplementary sketch \\
Internal/external ties & CHS \SI{457}{\milli\metre} $\times$ \SI{40}{\milli\metre} & Thickness not stated in the source \textbf{IS THIS THE DRAWINGS?}, so \SI{40}{\milli\metre} was assumed \\
High tie & Twin locked-coil strands, $2 \times \SI{115}{\milli\metre}$ & Modelled as one equivalent tie element with the same total cross-sectional area; prestressed to \SI{450}{\mega\pascal} \\
Straps & Equivalent rod area \SI{40000}{\milli\metre\squared} & Based on two \SI{500}{\milli\metre} $\times$ \SI{40}{\milli\metre} plates; bending stiffness neglected \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Assumptions}

The assumptions made were the use of a linear-elastic S355 steel model, uniform wall thicknesses where indicated, a projected tributary width of \SI{18}{\metre} for each transverse frame, automatic inclusion of self-weight through the material density, axial-only idealisation of the support-tree members, and modelling the twin high-tie strands as one prestressed tie element with the same total cross-sectional area.


\newpage
\section{Deliverable 3: Initial Model Validation}
\subsection{Assumptions}

The validation assumes a 2D in-plane frame subjected to vertical symmetric loading. Out-of-plane bending, torsion, adjacent-frame load sharing and detailed connection stiffness are neglected, so the check is intended only to confirm that the global model gives a realistic level of response rather than an exact local solution.


\subsection{Model Validation}
As the prestressed arch-and-tie behaviour is difficult to analyse by simple hand calculations, the initial validation was carried out on the non-prestressed model and focused on the bending moment at the tusk-to-arch connection. The loaded tusk was idealised as a \SI{25}{\metre} cantilever under the equivalent ULS line load $w = 1.35 \times 14.4 + 1.5 \times 10.8 = \SI{35.64}{\kilo\newton\per\metre}$, giving a support moment $M = wL^2/2 \approx \SI{11125}{\kilo\newton\metre}$, as shown in Figure~\ref{fig:validation_handcalc}. The corresponding FE result is about \SI{16490}{\kilo\newton\metre}, so the model predicts the correct order of magnitude for this key bending action. The difference is expected because the hand check neglects the stiffness of the curved arch, frame interaction between both sides of the roof, and the more complex redistribution captured by the FE model.

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{validation.png}
\caption{Hand validation of the tusk-to-arch connection bending moment.}
\label{fig:validation_handcalc}
\end{figure}

\subsection{Model Verification}
Basic verification checks were also completed: the GSA solver ran without instability warnings, the vertical reactions balanced the applied gravity loads, the response remained symmetric under symmetric loading, and the deformed shape was physically consistent, with the arch crown moving downward and the ties remaining in axial tension.

\subsection{Model Adequacy}
Taken together, the modelling assumptions, validation considerations and verification checks indicate that the global frame model is adequate for an initial study of the roof under symmetric vertical loading. However, it should still be understood as a simplified global model rather than a full representation of the three-dimensional structural behaviour.

\newpage
\section{Deliverable 4: Mesh Refinement Sensitivity Study}

\subsection{Convergence Study Methodology}
Three mesh densities were evaluated under the governing ULS vertical load combination $1.35G_k + 1.5Q_k + 1.0P_s$, where $P_s$ represents the prestress in the high-tie. Mesh 1 is the coarsest representation of the arch, Mesh 2 retains more intermediate points, and Mesh 3 preserves the full coordinate set taken from the supplementary geometry and is used as the reference model \textbf{DO YOU MEAN THE ONE WE CHOOSE IN THE END OR THE ONE WE USE TO BASE PREVIOUS STUFF FROM?}. To judge convergence, one displacement quantity and two force quantities were tracked: arch central deflection, high-tie axial force and arch central bending moment. These parameters give a useful picture of both overall stiffness and the redistribution of internal action as the curved geometry is refined.

\begin{table}[H]
\centering
\footnotesize
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.35}
\caption{Influence of arch mesh density on key global responses}
\label{tab:mesh_sensitivity}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}X >{\centering\arraybackslash}p{0.13\textwidth} >{\centering\arraybackslash}p{0.08\textwidth} >{\centering\arraybackslash}p{0.13\textwidth} >{\centering\arraybackslash}p{0.15\textwidth} >{\centering\arraybackslash}p{0.16\textwidth} >{\centering\arraybackslash}p{0.12\textwidth}}
\toprule
\textbf{Mesh} & \textbf{Arch elements} & \textbf{DoF} & \textbf{Arch Central Deflection (mm)} & \textbf{High-Tie Axial Force (kN)} & \textbf{Arch Central Bending Moment (kNm)} & \textbf{\% Deflection Change vs Mesh 3} \\
\midrule
Mesh 1 & 14 & 53 & 241.2 & 9725 & 12760 & 37.6\% \\
Mesh 2 & 16 & 59 & 175.7 & 9309 & 960.2 & 0.2\% \\
Mesh 3 & 22 & 77 & 175.3 & 9320 & 198.2 & 0.0\% \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Mesh1.png}

    \small Mesh 1
\end{minipage}
\hfill
\begin{minipage}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Mesh2.png}

    \small Mesh 2
\end{minipage}
\hfill
\begin{minipage}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Mesh3.png}

    \small Mesh 3
\end{minipage}
\caption{Bending-moment diagrams for the three mesh densities used in the sensitivity study.}
\label{fig:mesh_bending}
\end{figure}

A solution is considered converged when further refinement causes negligible change in the target quantity. The following strict convergence criterion was applied between iterations:
\begin{equation}
    \frac{|\delta_{n} - \delta_{n-1}|}{\delta_{n}} < 2\%
\end{equation}

Table \ref{tab:mesh_sensitivity} and Figure~\ref{fig:mesh_bending} show that the most coarse arch discretisation significantly distorts the response. If Mesh 3 is used as the baseline, Mesh 1 overestimates the arch central deflection by 37.6\%, the high-tie force by 4.3\% and the arch central bending moment by 6337.9\%. Moving from Mesh 1 to Mesh 2, the sign of bending moment changes across parts of the arch, showing that the coarsest mesh does not reproduce the qualitative moment distribution reliably. Mesh 2 brings the deflection error down to only 0.2\% and the tie-force difference to 0.1\%, but the central bending moment is still 384.5\% higher than in Mesh 3. This indicates that the global load path is already close to convergence by Mesh 2, whereas the bending response at the centre of the arch remains highly sensitive to how the curvature is represented. The very large percentage differences in bending moment are amplified by the small reference value in Mesh 3 (\SI{198.2}{\kilo\newton\metre}), \textbf{I DONT UNDERSTAND THIS PART} but they still show that a coarse geometric idealisation is not acceptable if bending results are to be used with confidence.

\subsection{Conclusion}
Based on these results, Mesh 3 is adopted for the remainder of the report. Although Mesh 2 is already adequate for deflection and tie force, only Mesh 3 captures the arch curvature accurately enough to stabilise the central bending moment, which is an important response quantity for interpreting the behaviour of the roof frame.

\newpage
\section{Deliverable 5: Influence of Prestressing Assumptions }

\subsection{Quantitative Comparison}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.25}
\caption{Simplified sensitivity of global response to high-tie prestress}
\label{tab:prestress_comparison}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.41\textwidth} >{\centering\arraybackslash}p{0.16\textwidth} >{\centering\arraybackslash}p{0.16\textwidth} >{\centering\arraybackslash}X}
\toprule
\textbf{Response Quantity} & \textbf{Prestress Neglected} & \textbf{Prestress Included} & \textbf{Change With Prestress} \\
\midrule
Maximum vertical displacement (mm) & 490 & 175.3 & $-64.2\%$ \\
Centre rafter bending moment (kNm) & 9811 & 198.2 & $-98.0\%$ \\
Tusk-to-arch connection bending moment (kNm) & 16490 & 5837 & $-64.6\%$ \\
High tie force (kN) & 4296 & 9320 & $+116.9\%$ \\
Horizontal reaction at supports (kN) & 4211 & 3480 & $-17.4\%$ \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Noprestress.png}

    \small Prestress Neglected
\end{minipage}
\hfill
\begin{minipage}[t]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{With_prestress.png}

    \small Prestress Included
\end{minipage}
\caption{Comparison of bending-moment response with and without high-tie prestress.}
\label{fig:prestress_bending}
\end{figure}


\subsection{Discussion of Results}
Using the updated values in Table \ref{tab:prestress_comparison} and Figure~\ref{fig:prestress_bending}, the influence of prestress is much stronger than in the earlier model \textbf{INITAL MODEL VALIDATION?}. Including prestress reduces the maximum vertical displacement from 490 mm to 175.3 mm, a reduction of 64.2\%. The centre-rafter bending moment falls from 9811 kNm to 198.2 kNm, which is a reduction of 98.0\%, while the tusk-to-arch connection bending moment reduces by 64.6\%. At the same time, the high-tie force rises from 4296 kN to 9320 kN, an increase of 116.9\%, and the horizontal support reaction reduces by 17.4\%. The structural behaviour therefore changes from a relatively bending-dominated frame response to a much more efficient arch-and-tie action once the high tie is activated. \textbf{COULD WRITE MORE? THIS JUST STATES TABLE VALUES}

\subsection{Implications for Design and Tolerance}
These updated results show that the prestress assumption is critical to both design interpretation and construction tolerance assessment. If prestress is neglected, the model greatly overestimates vertical deflection and rafter bending, but significantly underestimates the tensile force that must be carried by the high tie, its anchorage system and the connected support nodes. In other words, the controlling design actions move away from flexural demand in the rafters and towards force transfer in the tie system once prestress is included.

This conclusion is consistent with the supplementary construction sequence, where paired cable stressing, preset geometry, monitoring during stressing, staged load transfer and a final adjustment after creep were all used to control the final geometry and force state. The real roof is therefore expected to behave much closer to the prestressed model than to the unprestressed one. For the remainder of this report, the prestressed model is adopted as the minimum realistic representation of the completed roof, while recognising that a full construction-stage model would still be required if residual locked-in stress or erection tolerances were themselves critical outputs.

\newpage
\section{Deliverable 6: Boundary Conditions for More Detailed Analysis}

For a more detailed FE study, the rafter splice between the tusk rafter and arch rafter should be extracted from the global frame model and analysed as a local submodel. The purpose of the local model would be to resolve the stress distribution in the splice plates, bolts or welds, and the adjacent box and I-section walls, which cannot be captured accurately by the beam idealisation used in the global model.

The local model should include a short length of member on each side of the splice so that the boundary conditions are applied away from the connection itself. This reduces artificial stress concentrations caused by restraining the model too close to the detail. A practical choice would be to include approximately one section depth of the arch rafter and one section depth of the tusk rafter beyond the splice on either side.

\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{bc_connection.png}
\caption{Recommended boundary conditions for the local splice model}
\label{fig:placeholder}
\end{figure}

The preferred boundary condition is to impose the displacement and rotation components extracted from the global beam model at the two cut faces of the local model. In practice, each cut face can be coupled to a reference node so that the sectional deformation remains compatible with the global model. The six kinematic quantities imposed at each end are the three translations ($u_x$, $u_y$, $u_z$) and the three rotations ($r_x$, $r_y$, $r_z$). This approach is preferable to simply fixing one end, because it reproduces the actual deformation state of the connection and avoids introducing artificial restraint moments.

The corresponding section forces should also be checked against the global model outputs. The main actions expected at the splice are axial force $N$, vertical shear $V$, bending moment $M$, and a smaller torsional component due to the curved roof geometry and eccentricity of the connection detail. If the software or modelling approach makes direct displacement transfer difficult, an acceptable alternative is to apply the equivalent set of sectional resultants at the two cut faces, provided that overall equilibrium is maintained.

To prevent rigid body motion in the local model, one reference node may be lightly stabilised after the global displacements have been assigned. This stabilisation should only remove numerical singularities and must not alter the transferred load path. Contact, bolt preload, or weld representation can then be added depending on the intended level of detail. For the purposes of this coursework, the key requirement is that the splice boundaries are driven by the global model response rather than by arbitrary fixed supports.

Section cuts are located approximately  one local section depth  away from the splice connection along the axis of the member. This avoids local stress disturbance. As the depth varies along the rafter, the local section depth is equal to the splice-region depth (3.6 m), cuts A–A and B–B are placed approximately 3.6 m from the splice on either side. At both cut faces, the translational and rotational displacements extracted from the global FE model are imposed. This ensures compatibility between the local splice model and the global structural response.

\newpage
\begingroup
\small
\bibliographystyle{plain}
\bibliography{references}
\endgroup

\end{document}
